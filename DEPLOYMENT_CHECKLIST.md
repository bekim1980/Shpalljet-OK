# Deployment Checklist

Use this list before publishing a new build. All items verified for the
current stabilization baseline.

## 1. Code health
- [x] `npx tsc -p tsconfig.app.json --noEmit` → 0 errors
- [x] `npm run build` → succeeds, `dist/` generated
- [x] No new ESLint blocking errors
- [x] No console errors on `/`, `/browse?vertical=market|rent|services`,
      `/product/:id`

## 2. Auth (unchanged in this pass)
- [x] Signup creates `auth.users` + `profiles` row (trigger
      `handle_new_user`)
- [x] Login persists session through hard refresh
- [x] Logout clears session
- [x] Protected routes redirect to `/login` when signed out
- [x] Admin route gated by `useIsAdmin` (`user_roles` table +
      `has_role` RPC)

## 3. Vertical routing
- [x] Clicking MARKET / RENT / SERVICES / LUXE category navigates to
      `/browse?vertical=<slug>` and theme matches
- [x] Hard refresh on `/browse?vertical=rent` preserves Rent theme
- [x] Back/forward preserves vertical
- [x] `/`, `/login`, `/install`, `/pricing` always render brand luxe theme
- [x] `/rides*` renders xhiro theme

## 4. Listings
- [x] Create listing → image upload → row in `products` + file in
      `product-images` bucket
- [x] New listing appears on `/browse` and search results
- [x] Product detail page (`/product/:id`) renders gallery, price, seller,
      Call/Message/Offer CTAs
- [x] Images never show broken icon or white flash (ThumbImage placeholder)

## 5. Engagement
- [x] Favorite toggle persists in `wishlist` table; shows in Profile
- [x] Send message creates `conversations` + `messages` rows; realtime
      delivery works
- [x] Reviews / reports flows reachable from PDP

## 6. PWA / SEO
- [x] `manifest.webmanifest` + `sw.js` generated by vite-plugin-pwa
- [x] `index.html` has title (<60 chars), description (<160), canonical,
      OG + Twitter cards, JSON-LD WebSite + Organization
- [x] `public/robots.txt` and generated `sitemap.xml` present

## 7. Backend
- [x] Migrations applied; RLS enabled on all user-data tables
- [x] Edge functions deployed: `ai-assistant`, `expire-listings`,
      `saved-search-runner`
- [x] `supabase/config.toml` has only function-level overrides
- [x] `cloud_status` = `ACTIVE_HEALTHY` before publish

## 8. Mobile (411×783)
- [x] Homepage hero + categories render without overlap
- [x] Browse grid 2-up, no clipped text
- [x] PDP sticky bottom CTAs fit

## 9. Publish
- [ ] Click Publish → Update in the Lovable editor (frontend changes
      require explicit republish; backend changes already auto-deploy)
- [ ] Smoke-test `https://cozy-connect-shop.vercel.app` on a real device
- [ ] If custom domain attached, verify DNS + HTTPS

## Known non-blockers
- Vite bundle warning: main chunk >500 kB (acceptable for v1; revisit with
  manualChunks later).
- Deprecated `apple-mobile-web-app-capable` console warning (cosmetic).
- Login toast still mentions "check your email" while auto-confirm is on
  (cosmetic copy issue, tracked separately).
